<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F6FA"
    android:clipChildren="false"
    android:clipToPadding="false">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- HEADER -->
        <TextView
            android:text="Tasks"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:background="#173A6A"
            android:padding="16dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- SEARCH -->
        <EditText
            android:id="@+id/etSearch"
            android:hint="Cari task..."
            android:textSize="14sp"
            android:layout_margin="12dp"
            android:background="@drawable/bg_rounded"
            android:padding="12dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- 🔥 FILTER CHIP (FIXED) -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="18dp"
            android:paddingEnd="12dp"
            android:scrollbars="none"
            android:clipToPadding="false"
            android:clipChildren="false">

            <com.google.android.material.chip.ChipGroup
                android:id="@+id/chipGroupFilter"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:singleSelection="true"
                app:chipSpacingHorizontal="12dp">

                <!-- SEMUA -->
                <com.google.android.material.chip.Chip
                    android:id="@+id/chipAll"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:checkable="true"
                    android:elevation="4dp"
                    android:text="Semua"
                    android:textColor="@color/chip_all_text"
                    android:textStyle="bold"
                    app:checkedIconVisible="false"
                    app:chipBackgroundColor="@color/chip_all_bg"/>

                <!-- DONE -->
                <com.google.android.material.chip.Chip
                    android:id="@+id/chipDone"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:checkable="true"
                    android:elevation="4dp"
                    android:text="Done"
                    android:textColor="@color/chip_done_text"
                    android:textStyle="bold"
                    app:checkedIconVisible="false"
                    app:chipBackgroundColor="@color/chip_done_bg"/>

                <!-- PROGRESS -->
                <com.google.android.material.chip.Chip
                    android:id="@+id/chipProgress"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:checkable="true"
                    android:elevation="4dp"
                    android:text="Progress"
                    android:textColor="@color/chip_progress_text"
                    android:textStyle="bold"
                    app:checkedIconVisible="false"
                    app:chipBackgroundColor="@color/chip_progress_bg"/>

                <!-- OVERDUE -->
                <com.google.android.material.chip.Chip
                    android:id="@+id/chipOverdue"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:checkable="true"
                    android:elevation="4dp"
                    android:text="Overdue"
                    android:textColor="@color/chip_overdue_text"
                    android:textStyle="bold"
                    app:checkedIconVisible="false"
                    app:chipBackgroundColor="@color/chip_overdue_bg"/>

            </com.google.android.material.chip.ChipGroup>
        </HorizontalScrollView>

        <!-- LIST -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvTask"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipToPadding="false"
            android:paddingStart="8dp"
            android:paddingEnd="12dp"
            android:paddingBottom="120dp"/>

        <!-- BOTTOM NAV -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNav"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#FFFFFF"
            app:menu="@menu/bottom_nav_menu"/>

    </LinearLayout>

    <!-- FAB -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btnAddTask"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="10dp"
        android:layout_marginBottom="105dp"
        android:src="@android:drawable/ic_input_add"
        app:backgroundTint="#173A6A"/>

</FrameLayout>